import {getAllPosts,CATEGORIES,catSlug} from '../../lib/posts';import PostCard from '../../components/PostCard';import Link from 'next/link';
export const metadata={title:'All posts'};
export default function AP(){const posts=getAllPosts();return(<div><h1 className="font-serif text-4xl mb-3">All posts</h1><p className="opacity-80 mb-8">Everything we\'ve written so far. Filter by category below.</p><div className="flex flex-wrap gap-2 mb-10"><Link href="/posts" className="px-3 py-1.5 bg-forest text-bg text-sm rounded-full">All</Link>{CATEGORIES.map(c=>(<Link key={c} href={`/category/${catSlug(c)}`} className="px-3 py-1.5 bg-sand dark:bg-ink/40 text-sm rounded-full">{c}</Link>))}</div><div className="grid sm:grid-cols-2 gap-5">{posts.map(p=><PostCard key={p.slug} p={p}/>)}</div></div>);}