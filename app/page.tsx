import Link from 'next/link';import {getAllPosts,CATEGORIES,catSlug} from '../lib/posts';import PostCard from '../components/PostCard';import Newsletter from '../components/Newsletter';
export default function H(){const posts=getAllPosts();const featured=posts[0];const rest=posts.slice(1);return(<div><section className="mb-12"><p className="text-sm uppercase tracking-widest text-forest mb-3">Doddy.in</p><h1 className="font-serif text-4xl sm:text-6xl mb-4 leading-tight">Real grooming talk for Indian men.</h1><p className="text-lg opacity-80 max-w-2xl">No sponsored fluff. No recycled Western listicles. Just what works for the way we actually live, sweat, and show up.</p></section><div className="flex flex-wrap gap-2 mb-10">{CATEGORIES.map(c=>(<Link key={c} href={`/category/${catSlug(c)}`} className="px-3 py-1.5 bg-sand dark:bg-ink/40 text-sm rounded-full hover:bg-forest hover:text-bg transition">{c}</Link>))}</div>{featured&&(<Link href={`/posts/${featured.slug}`} className="block mb-12 p-7 bg-sand dark:bg-ink/40 rounded-lg border-l-4 border-accent"><p className="text-xs uppercase tracking-wider text-forest mb-2">Featured · {featured.category}</p><h2 className="font-serif text-3xl mb-3">{featured.title}</h2><p className="opacity-80">{featured.excerpt}</p><p className="text-xs opacity-60 mt-3">{featured.readingTime}</p></Link>)}<h2 className="font-serif text-2xl mb-5">Recent posts</h2><div className="grid sm:grid-cols-2 gap-5">{rest.map(p=><PostCard key={p.slug} p={p}/>)}</div><Newsletter/></div>);}