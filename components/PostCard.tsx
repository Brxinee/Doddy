import Link from 'next/link';import {Post,catSlug} from '../lib/posts';
export default function PostCard({p}:{p:Post}){return(<article className="border border-sand dark:border-ink/40 p-5 rounded-lg hover:border-accent transition"><Link href={`/category/${catSlug(p.category)}`} className="inline-block text-xs uppercase tracking-wider text-forest font-medium mb-2">{p.category}</Link><Link href={`/posts/${p.slug}`}><h3 className="font-serif text-xl mb-2">{p.title}</h3></Link><p className="text-sm opacity-80 mb-3">{p.excerpt}</p><p className="text-xs opacity-60">{p.readingTime}</p></article>);}